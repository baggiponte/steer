# Domain Docs

Before exploring the codebase, read `CONTEXT.md` and relevant decisions in
`docs/adr/` when they exist. Missing files require no special handling.

This is a single-context repository:

```text
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

Use terminology defined in `CONTEXT.md`. Surface conflicts with existing ADRs
instead of silently overriding them.
